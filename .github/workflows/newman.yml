name: Newman Run

on: 
  workflow_dispatch:
  push:

jobs:
  newman:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: matt-ball/newman-action@master
        with:
          collection: Integrate_GitHubAction.json
